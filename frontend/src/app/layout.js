import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata = {
  title: "AI Data Validation System — Multi-Domain Mainframe Processing",
  description:
    "Enterprise data validation platform supporting Banking, Healthcare, and E-Commerce domains with automated mainframe batch job simulation and comprehensive quality scoring.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
